name: Actu_bot_3d 🤖

on:
  schedule:
    - cron: '0 7 * * *'  # 8h00 France
    - cron: '0 19 * * *' # 20h00 France
  workflow_dispatch:      # Pour lancer à la main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Récupérer le code
        uses: actions/checkout@v3

      - name: Installer Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Installer les outils
        run: pip install requests feedparser google-generativeai

      - name: Lancer le bot
        run: |
          if [ "${{ github.event.schedule }}" == "0 7 * * *" ]; then
            python bot_telegram.py matin
          else
            python bot_telegram.py soir
          fi
